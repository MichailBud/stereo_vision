# Stereo Vision

**Курсовой проект** по предмету *Методы локализации, позиционирования и навигации*

## Тема проекта

**Алгоритм оценки положения мобильного робота по данным системы стереозрения**

### Цель
Локализация робота на основе триангуляции точек пространства с двух камер.

### Исходные данные
- Входные данные: стереопара изображений

### Задание
- Реализовать алгоритм поиска соответствий на эпиполярных линиях
- Построение карты диспаратности
- Самостоятельно вычислить 3D-координаты характерных точек сцены
- На их основе оценить перемещение камеры

### Итоговый результат
Оценка 6-DOF положения робота (позиция и ориентация).

### Технический стек
- ROS2 Humble
- Gazebo Classic
- Ubuntu 22.04

---

## Структура проекта

```
stereo_vision/
├── models/          # Модели для Gazebo
├── worlds/          # Миры для Gazebo (.world файлы)
├── info/            # Дополнительная информация и документация
└── src/             # ROS2 пакеты
    ├── images_processing/   # Обработка стереопары, SGBM, карта диспаратности
    │   ├── src/             # Исходный код узла
    │   ├── launch/          # Launch-файлы с параметрами
    │   └── CMakeLists.txt
    ├── triangulation_cloud/ # 3D реконструкция, облако точек
    │   ├── src/             # Узел триангуляции
    │   └── CMakeLists.txt
    ├── keyboard_control/    # Управление роботом с клавиатуры
    └── test/                # Тестовый пакет
```

---

## Реализованный функционал

### ✅ Обработка стереоизображений
- Публикация выпрямленных изображений с левой и правой камеры
- Вычисление карты диспаратности алгоритмом **StereoSGBM**
- Встроенная speckle-фильтрация SGBM

### ✅ Настроенные параметры SGBM
| Параметр | Значение | Описание |
|----------|----------|----------|
| `min_disparity` | 0 | Минимальная диспаратность |
| `num_disparities` | 64 | Диапазон поиска (кратно 16) |
| `block_size` | 7 | Размер окна сопоставления |
| `P1` | 20 | Штраф за изменение на ±1 |
| `P2` | 70 | Штраф за изменение > ±1 |
| `disp12_max_diff` | 10 | Left-right проверка |
| `uniqueness_ratio` | 10 | Фильтр уникальности |
| `speckle_window_size` | 75 | Размер speckle-окна |
| `speckle_range` | 4 | Диапазон speckle |

### ✅ 3D реконструкция
- Триангуляция точек по карте диспаратности
- Публикация облака точек (`/point_cloud2`)
- Визуализация в RViz

> **Важно:** OpenCV StereoSGBM возвращает disparity в формате `CV_16S` с субпиксельной точностью 1/16 (значения умножены на 16). При вычислении 3D-координат disparity делится на 16.0 для получения корректного масштаба в метрах: `Z = (B * fx) / (disp / 16.0)`.

### ✅ Топики
| Топик | Тип | Описание |
|-------|-----|----------|
| `/left_camera` | `sensor_msgs/Image` | Выпрямленное левое изображение |
| `/right_camera` | `sensor_msgs/Image` | Выпрямленное правое изображение |
| `/disparity` | `sensor_msgs/Image` | Карта диспаратности (нормализованная) |
| `/point_cloud2` | `sensor_msgs/PointCloud2` | 3D облако точек |

---

## Запуск визуализации

**RViz:**
```bash
rviz2
```

Добавьте displays:
- **Image** → Topic: `/disparity`
- **PointCloud2** → Topic: `/point_cloud2`, Fixed Frame: `camera_frame`

## Требования

- ROS2 (Humble/Iron/Jazzy)
- Gazebo Classic или Gazebo Sim
- CMake

## Сборка

```bash
cd stereo_vision
colcon build --symlink-install
source install/setup.bash
```

## Запуск

```bash
ros2 launch test test_launch.py
```

## Управление роботом

Пакет `keyboard_control` позволяет управлять роботом с клавиатуры.

**Доступ к клавиатуре:**

```bash
# Дать права на устройство ввода (после перезагрузки права сбрасываются)
sudo chmod 777 /dev/input/by-path/platform-i8042-serio-0-event-kbd

# Или найти своё устройство
ls -la /dev/input/event*
sudo chmod 777 /dev/input/eventX  # замени X на номер устройства
```

**Запуск:**

```bash
ros2 run keyboard_control keyboard_control_node
```

**Управление:**
- **W / ↑** — вперёд
- **S / ↓** — назад
- **A / ←** — влево
- **D / →** — вправо

---

## Модель робота

Проект включает модель **Robot Cameron** — робот-пылесос с дифференциальным приводом.

**Параметры:**
| Компонент | Параметр | Значение |
|-----------|----------|----------|
| Корпус | Форма | Цилиндр |
| | Радиус | 20 см |
| | Высота | 10 см |
| | Масса | 14.77 кг |
| Колёса | Количество | 2 (ведущие) |
| | Радиус | 5 см |
| | Длина | 2 см |
| | Расстояние между колёсами | 26 см |
| | Масса каждого | 0.18 кг |
| Шар-кастер | Радиус | 5 см |
| | Расположение | Сзади |
| | Масса | 5 кг |
| | Тип соединения | Ball joint |

**Привод:** Дифференциальный (два независимо управляемых колеса)

## Лицензия

MIT